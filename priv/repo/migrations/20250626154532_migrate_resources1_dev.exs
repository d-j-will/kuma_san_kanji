defmodule KumaSanKanji.Repo.Migrations.MigrateResources1 do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:users) do
      remove(:joined_at)
      remove(:hashed_password)
      modify(:username, :citext)

      add(:created_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
      )

      add(:updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
      )
    end

    create table(:tokens, primary_key: false) do
      add(:jti, :text, null: false, primary_key: true)
      add(:subject, :text, null: false)
      add(:expires_at, :utc_datetime, null: false)
      add(:purpose, :text, null: false)
      add(:extra_data, :map)

      add(:created_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
      )

      add(:updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
      )
    end
  end

  def down do
    drop(table(:tokens))

    alter table(:users) do
      remove(:updated_at)
      remove(:created_at)
      modify(:username, :text)
  # Relax NOT NULL so reverse operations in codegen don't fail if values missing
  add(:hashed_password, :text)
      add(:joined_at, :utc_datetime, default: fragment("(now() AT TIME ZONE 'utc')"))
    end
  end
end
